extern crate aoc;

use aoc::run_for_files;
use aoc::y2024::d08::resonant_collinearity::part1;

fn main() {
    run_for_files(2024, 8, &["sample", "input"], &part1);
    // run_for_files(2024, 7, &["sample", "input"], &part2);
}
