extern crate aoc;

use aoc::run_for_files;
use aoc::y2024::d09::disk_fragmenter::part1;

fn main() {
    run_for_files(2024, 9, &["sample", "input"], &part1);
    // run_for_files(2024, 9, &["sample", "input"], &part2);
}
